version: '3.6'
services:
  api:
    build:
      context: ../api
      target: production
    image: cekta/youtube-minimal-knowledge:api
  web:
    build:
      context: ../web
      target: production
    image: cekta/youtube-minimal-knowledge:web
